# WgShadertoy examples

Examples for [wgshadertoy](https://github.com/fralonra/wgshadertoy).

You can load from `wgs` file directly in `wgshadertoy`.

Each example also provides the raw `wgsl` file and textures.
